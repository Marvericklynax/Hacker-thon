import 'package:flutter/material.dart';

class MentorshipScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Mentorship Screen'),
      ),
      body: Center(
        child: Text('Mentorship details will be here'),
      ),
    );
  }
}
